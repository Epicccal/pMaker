# examples/ip_fragment/gre_checksum_bad_frag.yaml
#
# GRE checksum 覆盖值 + 内层分片:该路径关掉自动计算,各片 checksum 同为
# 声明值 0xdead —— 覆盖值不被分片重序列化改回,是「畸形不被修正」在分片
# 路径上的回归点。
link_type: ethernet
packets:
  - stack:
      - eth:  { src: "00:11:22:33:44:55", dst: "66:77:88:99:aa:bb" }
      - ipv4: { src: "1.1.1.1", dst: "2.2.2.2", ttl: 64 }
      - gre:  { checksum: 0xdead }                           # 隐含 C=1,原样落值
      - ipv4: { src: "192.168.1.1", dst: "192.168.1.2", ttl: 64, mtu: 44 }
      - udp:  { sport: 40000, dport: 65001 }
      - payload:
          payload: "0123456789012345678901234567" # 28 字节,内层数据报 56 > 44
